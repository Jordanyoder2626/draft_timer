[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "draft-timer"
version = "0.1.0"
description = "Fantasy football draft timer React application."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
py-modules = []
